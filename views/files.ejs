<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Your Uploaded Files</title>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-100 font-sans p-6">
  <div class="max-w-4xl mx-auto bg-white p-6 rounded shadow">
    <h1 class="text-2xl font-bold mb-4">Your Uploaded Files</h1>
    <% if (files.length === 0) { %>
      <p>You have not uploaded any files yet.</p>
    <% } else { %>
      <table class="min-w-full border border-gray-300">
        <thead>
          <tr class="bg-gray-200">
            <th class="border border-gray-300 px-4 py-2 text-left">Filename</th>
            <th class="border border-gray-300 px-4 py-2 text-left">Original Name</th>
            <th class="border border-gray-300 px-4 py-2 text-left">Size (KB)</th>
            <th class="border border-gray-300 px-4 py-2 text-left">Uploaded At</th>
            <th class="border border-gray-300 px-4 py-2 text-left">Download</th>
          </tr>
        </thead>
        <tbody>
          <% files.forEach(function(file) { %>
            <tr>
              <td class="border border-gray-300 px-4 py-2"><%= file.filename %></td>
              <td class="border border-gray-300 px-4 py-2"><%= file.originalname %></td>
              <td class="border border-gray-300 px-4 py-2"><%= (file.size / 1024).toFixed(2) %></td>
              <td class="border border-gray-300 px-4 py-2"><%= file.uploadDate.toLocaleString() %></td>
              <td class="border border-gray-300 px-4 py-2">
                <a href="/uploads/<%= file.filename %>" download class="text-blue-600 hover:underline">Download</a>
              </td>
            </tr>
          <% }); %>
        </tbody>
      </table>
    <% } %>
  </div>
</body>
</html>
